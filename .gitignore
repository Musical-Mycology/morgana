node_modules
.next
out
*.tsbuildinfo
next-env.d.ts
test-results
playwright-report

# Real decks are private user data — never commit them.
/data/decks/*
!/data/decks/.gitkeep

# Smoke-test scratch volumes (project-relative; /tmp does not bind-mount on Docker Desktop)
.smoke-data*

.superpowers/

# Per-repo mm-beholder usage telemetry (mirrors ~/.claude/usage/tally.jsonl)
.claude/token-tally.jsonl
